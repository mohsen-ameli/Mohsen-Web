from django.urls import path

#from .views import post_create_comment, post_detail_comment


app_name = "comment"
urlpatterns = [
    #path('comment/', post_create_comment, name="post-comment"),
    #path('blog/<str:slug>/<int:id>', post_detail_comment, name="post-detail-comment"),
]
